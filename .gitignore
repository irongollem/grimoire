# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/

# Environment variables
.env.local
.env.*.local
.env.test

# Supabase edge-function secret files (Stripe keys etc.) — never commit
supabase/.env*
# Local DB seed pulled from remote (tester data) — never commit to the public repo
supabase/seed.sql

# Personal setup notes (gitignored — edit freely)
TODO_SETUP.md

# Claude Code — commit project settings, ignore personal overrides
.claude/settings.local.json

# Supabase CLI temp files (project metadata, not secrets but not needed in repo)
supabase/.temp/
# Supabase CLI local-branch tracking artifact
supabase/.branches/

# Campaign-specific NPC importer sidecars — content for a private campaign, not software
scripts/chapter_npc_configs/

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.mcp.json
src/views/play/TEMPSHIT.ts
.claude/worktrees/

# vite-ssg build temp directory
.vite-ssg-temp/

# Local art working directory. Source PNGs + webp exports for canonical art.
# The shipped assets live in Supabase storage (downtime-images/srd/), so these
# are working files, not build inputs — keeping 17MB of raw PNGs out of a public
# repo. Back them up outside git if you want the originals.
art-src/
.vercel
.env*
# Vite's isolated localdb env contains only the Supabase CLI's public defaults.
!config/env/localdb/.env

# graphify knowledge-graph output. Not committed, for two reasons beyond size:
# every node carries a `source_location` and the manifest a `scan_root`, so the
# files are full of absolute local paths (/Users/<name>/...) — and this repo is
# public. The graph is also a derived artifact: `/graphify .` rebuilds it, and a
# committed copy would silently rot against the code it describes. Includes the
# extraction cache and cost.json (token spend), which are per-machine anyway.
graphify-out/
